[project]
name = "mcp-signal"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.0,<4",
    "signal-export>=3.8.3",
]

[tool.ruff]
target-version = "py313"
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
